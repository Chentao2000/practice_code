class Solution {
    public int findMin(int[] nums) 
    {
        int temp = nums[0];
        for (int i = 1; i < nums.length; i++)
            if (nums[i] < temp)
                return nums[i];
        return temp;
    }
}
